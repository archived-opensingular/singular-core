{
  "idMap": "${idMap}",
  "idLat": "${idLat}",
  "idLng": "${idLng}",
  "idZoom": "${idZoom}",
  "readOnly" : "${readOnly}",
  "idClearButton" : "${idClearButton}",
  "idCurrentLocationButton" : "${idCurrentLocationButton}"
}